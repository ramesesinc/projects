"entity.barangay|doBuild|doCancel" [mode=='init' || mode=='done']{
    enabled:false;
}
"entity.barangay|doBuild" [mode=='init' || mode=='done']{
    enabled:true;
}
"entity.barangay|doBuild" [mode == 'building']{
    enabled:false;
}
"doCancel" [mode == 'building']{
    enabled:true;
}
"loadingicon" [mode == 'building']{
    visible: true;
}
"loadingicon" [mode != 'building']{
    visible: false;
}
"doBuild|doCancel" [mode == 'done']{
    enabled:false;
}