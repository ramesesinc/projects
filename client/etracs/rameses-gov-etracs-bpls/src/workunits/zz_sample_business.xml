<workunit>
    <invokers>
        <invoker type="home.action" caption="Sample" target="popup" />
        <invoker type="formActions" caption="Back" action="back" />
        <invoker type="formActions" caption="Back 1" action="_default" />
   </invokers>
    
    <code>
        <![CDATA[    
        import com.rameses.rcp.annotations.*
        import com.rameses.rcp.common.*
        import com.rameses.osiris2.client.*
        import com.rameses.osiris2.common.*
        import com.rameses.common.*;
        import java.rmi.server.*
        import com.rameses.util.*;
        
        class ViewBusinessReceiptController {
        
            @Binding
            def binding;
            
            def lessor;
            boolean editmode;
        
            def entity = [lastname:'nazareno', firstname:'elmo'];
            def amount = 45000.25;
            
            def onselectLessor = { o->
                MsgBox.alert('on select lessor' + o );
            }
            
            def breakdown = [
                [caption:"500.00",denomination:500.00,qty:4,amount:200.0],
                [caption:"20.00",denomination:20.00,qty:3,amount:60.00],
            ];
            
            def handler = { o->
                MsgBox.alert("handled name is " + o);
                //entity.business = o;
                //binding.fireNavigation("page1");
            }
            
            def back() {
                return "default";
            }
            
            def fldHandler = { name, o->
                println "name " + name + " value: " +o;
            }
            
            def showData() {
                //MsgBox.alert( lessor );
                MsgBox.alert( breakdown );
            }
            
            void changeMode() {
                editmode = !editmode;
            }
        }
        
        ]]>
    </code>
    
    <pages>
        <page template="com.rameses.bpls.test.ZZSamplePage2"/>
        <page name="page1" template="com.rameses.bpls.test.ZZSamplePage"/>
    </pages>
    
</workunit>