<?xml version="1.0" encoding="UTF-8"?>
<workunit>
    <invokers>
    </invokers>
    
    <code>
        
<![CDATA[

import com.rameses.rcp.common.*;
import com.rameses.rcp.annotations.*;
import com.rameses.osiris2.client.*;
import com.rameses.osiris2.common.*;
import com.rameses.common.*;

public class MasterSyncDataController  
{
    @Binding
    def binding;
    
    
    @Service('MunicipalityMasterDataSyncService')
    def syncSvc;

    @Caller
    def caller;
    
    @Invoker 
    def inv;
    
    def service;
    def processing;
    boolean showry = false;
    boolean showsync = false;
    
    def params = [:];
    
    def getTitle(){
        return 'Synchronizing ' + inv.properties.entityName + ' data.'
    }
    
    def loghandler = new TextWriter();
    
    def handler = [
        onMessage : { data ->
            if (data != AsyncHandler.EOF){
                data.each{ 
                    service.save(it);
                    loghandler.writeln('Updating data : ' + (it.name ? it.name : it.code) + '.');
                }
            }
            else{
                caller?.search();
                loghandler.writeln('\nData has been successfully synchronized.');
                processing = false;
            }
            binding.refresh('doClose')
        },
        
        onError : { e ->
            println '='*50 ;
            println e.printStackTrace();
            loghandler.writeln('\n');
            loghandler.writeln('ERROR: ' + e.message);
            processing = false;
            binding.refresh('doClose|syncData')
        }
    ] as AsyncHandler

    
    def process = [
        run : {
            processing = true;
            loghandler.writeln('Connecting to remote server...');
            syncSvc.syncData(params, handler);
        }
    ] as Runnable;
    
    void syncData(){
        params.master = inv.properties.entityName;
        params.itemname = inv.properties.itemName;
        params.itemfield = inv.properties.itemField;
        params.itemkey = inv.properties.itemKey;
        
        if (! params.master) 
            throw new Exception('Entity name is not specified in the invoker.')
            
        if (MsgBox.confirm('Sync data from province?')){
            new Thread(process).start();
        }
    }
    
    def doClose(){
        return '_close';
    }
        
    def getService(){
        if (! service){
            def servicename = inv.properties.serviceName;
            if (! servicename)
                throw new Exception('Service Name is not specified in the invoker..');
            service = InvokerProxy.instance.create( servicename );
        }
        return service;
    }
        
        
}

]]>

    </code>
    <pages>
        <page template="com.rameses.gov.etracs.rptis.municipality.sync.SyncStatusPage"/>
    </pages>
</workunit>
