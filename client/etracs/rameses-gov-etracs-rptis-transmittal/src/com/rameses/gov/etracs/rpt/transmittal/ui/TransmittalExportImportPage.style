"file|export"[processing == false]{
    enabled:true;
}

"file|export"[processing == true]{
    enabled:false;
}

"export" [type != 'export']{
    visible:false;
}

"doImport" [type != 'import']{
    visible:false;
}