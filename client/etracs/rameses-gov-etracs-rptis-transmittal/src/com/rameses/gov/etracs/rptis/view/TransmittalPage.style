"entity.remarks" [mode == 'read']{
    enabled:false;
}

"entity.remarks" [mode != 'read']{
    enabled:true;
}

"selectedItem" [mode == 'read']{
    readonly:true;
}

"selectedItem" [mode != 'read']{
    readonly:false;
}
