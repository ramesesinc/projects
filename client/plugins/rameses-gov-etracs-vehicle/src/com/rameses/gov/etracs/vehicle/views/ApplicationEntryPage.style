"entity.*" [ entity.apptype.matches('RENEW|DROP') == true ] {
    enabled:false;
}

"entity.*" [ entity.apptype.matches('RENEW|DROP') != true ] {
    enabled:true;
}

"entity.(appyear|appdate)" [ entity.txnmode != 'CAPTURE' ] {
    enabled: false;
}