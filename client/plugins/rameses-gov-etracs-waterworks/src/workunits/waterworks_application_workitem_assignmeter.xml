<workunit>
    <invokers>
        <invoker type="waterworks_application:form:formActions" 
                 visibleWhen="#{task.state=='for-installation' &amp;&amp; task.assignee?.objid!=null}"
                 caption="Assign Meter"
                 target="popup"
                 action="init"
                 />
    </invokers>
    <code>
        <![CDATA[
        import com.rameses.rcp.common.*;
        import com.rameses.rcp.annotations.*;
        import com.rameses.osiris2.client.*;
        import com.rameses.osiris2.common.*;
        import com.rameses.common.*;
        import com.rameses.util.*;
        
        class AssignMeter {
        
            @Service("PersistenceService")
            def persistenceSvc;

            @Caller
            def caller;
            
            def info = [_schemaname:'waterworks_application'];
            
            void init() {
                def e = caller.entity;
                info.meter = e.meter;
                info.installer = e.installer;
                info.dtinstalled = e.dtinstalled;
                info.initialreading = e.initialreding;
            }
            
            def doCancel() {
                return "_close";
            }          
                           
            def doOk() {
                persistenceSvc.update(info);
                caller.entity.putAll( info );
                caller.refresh();
                return "_close";
            }                              
        }
        
        ]]>
    </code>
    <pages>
        <page template="com.rameses.gov.etracs.waterworks.views.AssignMeterPage"/>
    </pages>
</workunit>
