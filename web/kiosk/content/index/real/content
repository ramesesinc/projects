<% 
    def rpt = [];
    rpt << [ links:'rptbill',  name:'Billing']; 

%>



  <div id="wrapper"> 
    <header>
      <h1>Please Select Transaction</h1>
    </header>
      <div id="content">
        <div class="container">
          <div class="container-content">
            <div class="row">
              <% rpt.each{k->%>
                <div class="col-sm-4">
              <form method="get" action="/index/${k.links}">
                  <button type="submit" class="thumbnail">${k.name}</button>
              </form>
              </div>
                <!--<div class="col-sm-4">
                  <div>
                      <button r:context="kiosk" r:name="goToPage" r:param_link="${k.links}" class="thumbnail">${k.name}</button>
                  </div>
                </div>-->
              <%}%>
            </div>
          </div>
        </div>
      </div>  
  </div>

